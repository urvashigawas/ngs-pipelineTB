nextflow.enable.dsl = 2

params {
    reads = "data/*.fastq.gz"
    reference = "reference/H37Rv.fasta"
    outdir = "results"
}

process {
    executor = "local"
    cpus = params.cpus
}

params.cpus = 4
